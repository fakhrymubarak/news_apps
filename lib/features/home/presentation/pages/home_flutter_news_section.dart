part of 'home_page.dart';

class _HomeFlutterNewsSection extends StatelessWidget {
  const _HomeFlutterNewsSection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
